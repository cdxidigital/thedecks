'use client'

import Image from 'next/image'
import useSWR from 'swr'
import { useEffect, useMemo, useRef, useState } from 'react'
import { Bot, CloudUpload, Disc3, Library, LoaderCircle, LogOut, Menu, Pause, Play, Plus, Search, SlidersHorizontal, Sparkles, Trash2, X } from 'lucide-react'
import { authClient } from '@/lib/auth-client'

type Track={id:string;title:string;artist:string;fileName:string;duration:number|null;bpm:number|null;musicalKey:string|null;size:number;createdAt:string}
type Deck={track:Track|null;playing:boolean;current:number;duration:number;volume:number;rate:number;loopStart:number|null;loopEnd:number|null}
const fetcher=(url:string)=>fetch(url).then(async r=>{if(!r.ok)throw new Error((await r.json()).error||'Request failed');return r.json()})
const blank=():Deck=>({track:null,playing:false,current:0,duration:0,volume:.8,rate:1,loopStart:null,loopEnd:null})
const time=(n:number)=>`${Math.floor((n||0)/60)}:${String(Math.floor((n||0)%60)).padStart(2,'0')}`

function Wave({progress}:{progress:number}){const bars=[28,46,64,35,72,51,82,42,69,31,57,76,43,66,38,85,53,71,47,62,33,79,55,68,41,74,49,88,37,64,52,77,45,69,34,81,58,72,40,61];return <div className="waveform" aria-label="Playback progress">{bars.map((h,i)=><i key={i} style={{height:`${h}%`,background:i/bars.length<progress?'var(--primary)':undefined}}/>)}<span style={{left:`${Math.min(100,progress*100)}%`}}/></div>}

function DeckView({index,deck,audio,onDeck,onLoad,tracks}:{index:number;deck:Deck;audio:(el:HTMLAudioElement|null)=>void;onDeck:(d:Deck)=>void;onLoad:(t:Track)=>void;tracks:Track[]}){const letter=String.fromCharCode(65+index);async function toggle(){if(!deck.track)return;const el=document.querySelector<HTMLAudioElement>(`#deck-audio-${index}`);if(!el)return;if(el.paused){await el.play()}else el.pause()}return <section className="panel deck-panel" aria-label={`Deck ${letter}`}><audio id={`deck-audio-${index}`} ref={audio} src={deck.track?`/api/tracks/${deck.track.id}/stream`:undefined} preload="metadata" onPlay={()=>onDeck({...deck,playing:true})} onPause={()=>onDeck({...deck,playing:false})} onLoadedMetadata={e=>onDeck({...deck,duration:e.currentTarget.duration||0})} onTimeUpdate={e=>{const el=e.currentTarget;if(deck.loopStart!==null&&deck.loopEnd!==null&&el.currentTime>=deck.loopEnd)el.currentTime=deck.loopStart;onDeck({...deck,current:el.currentTime,duration:el.duration||0})}}/><header><b className="deck-letter">{letter}</b><div><h2>{deck.track?.title||'Empty deck'}</h2><p>{deck.track?.artist||'Load a track from your library'}</p></div><strong>{deck.track?.bpm?.toFixed(1)||'—'}<small>{deck.track?.musicalKey||'KEY —'}</small></strong></header><Wave progress={deck.duration?deck.current/deck.duration:0}/><input className="seek" aria-label={`Deck ${letter} position`} type="range" min="0" max={deck.duration||1} step="0.1" value={deck.current} disabled={!deck.track} onChange={e=>{const el=document.querySelector<HTMLAudioElement>(`#deck-audio-${index}`);if(el)el.currentTime=Number(e.target.value);onDeck({...deck,current:Number(e.target.value)})}}/><div className="deck-time"><span>{time(deck.current)}</span><span>-{time(Math.max(0,deck.duration-deck.current))}</span></div><div className="transport-row"><button className="transport" disabled={!deck.track} onClick={()=>{const el=document.querySelector<HTMLAudioElement>(`#deck-audio-${index}`);if(el)el.currentTime=0}}>CUE</button><button className="transport play" disabled={!deck.track} onClick={toggle} aria-label={deck.playing?'Pause':'Play'}>{deck.playing?<Pause/>:<Play/>}</button><button className="transport" disabled={!deck.track} onClick={()=>onDeck({...deck,loopStart:deck.current,loopEnd:Math.min(deck.duration,deck.current+8)})}>LOOP</button><label>SPEED<input aria-label={`Deck ${letter} speed`} type="range" min="0.75" max="1.25" step="0.01" value={deck.rate} onChange={e=>{const rate=Number(e.target.value);const el=document.querySelector<HTMLAudioElement>(`#deck-audio-${index}`);if(el)el.playbackRate=rate;onDeck({...deck,rate})}}/></label></div>{!deck.track&&tracks.length>0&&<button className="deck-load" onClick={()=>onLoad(tracks[0])}>Load first library track</button>}</section>}

export function DJWorkspace({user}:{user:{name:string;email:string}}){const {data:tracks=[],error,isLoading,mutate}=useSWR<Track[]>('/api/tracks',fetcher);const [decks,setDecks]=useState<Deck[]>([blank(),blank(),blank(),blank()]);const [deckCount,setDeckCount]=useState<2|4>(2);const [mobileDeck,setMobileDeck]=useState(0);const [query,setQuery]=useState('');const [crossfade,setCrossfade]=useState(50);const [uploading,setUploading]=useState(false);const [assistant,setAssistant]=useState('Ask for a transition, set-building, or harmonic-mixing recommendation.');const [asking,setAsking]=useState(false);const [mobilePanel,setMobilePanel]=useState<'decks'|'library'|'ai'>('decks');const fileRef=useRef<HTMLInputElement>(null);const audioRefs=useRef<(HTMLAudioElement|null)[]>([]);
const filtered=useMemo(()=>tracks.filter(t=>`${t.title} ${t.artist}`.toLowerCase().includes(query.toLowerCase())),[tracks,query]);
useEffect(()=>{audioRefs.current.forEach((el,i)=>{if(!el)return;const side=i%2===0?Math.min(1,(100-crossfade)/50):Math.min(1,crossfade/50);el.volume=Math.max(0,Math.min(1,decks[i].volume*side))})},[crossfade,decks]);
function update(i:number,next:Deck){setDecks(d=>d.map((x,j)=>i===j?next:x))}function load(i:number,t:Track){const el=audioRefs.current[i];if(el){el.pause();el.currentTime=0}update(i,{...blank(),track:t})}
async function upload(file:File){setUploading(true);try{const duration=await new Promise<number>(resolve=>{const a=document.createElement('audio');const url=URL.createObjectURL(file);a.onloadedmetadata=()=>{resolve(Number.isFinite(a.duration)?a.duration:0);URL.revokeObjectURL(url)};a.onerror=()=>resolve(0);a.src=url});const base=file.name.replace(/\.[^.]+$/,'');const parts=base.split(' - ');const form=new FormData();form.set('file',file);form.set('title',parts.length>1?parts.slice(1).join(' - '):base);form.set('artist',parts.length>1?parts[0]:'Unknown Artist');form.set('duration',String(duration));const res=await fetch('/api/tracks',{method:'POST',body:form});if(!res.ok)throw new Error((await res.json()).error);await mutate()}catch(e){setAssistant(e instanceof Error?e.message:'Upload failed')}finally{setUploading(false)}}
async function remove(id:string){if(!confirm('Delete this track from your private library?'))return;const res=await fetch(`/api/tracks?id=${id}`,{method:'DELETE'});if(res.ok){setDecks(d=>d.map(x=>x.track?.id===id?blank():x));await mutate()}}
async function ask(prompt:string){setAsking(true);try{const res=await fetch('/api/assistant',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt,tracks})});const json=await res.json();setAssistant(json.text||json.error)}finally{setAsking(false)}}
return <main className="app-shell"><header className="topbar"><div className="brand"><span className="brand-mark"><Image src="/decks-4music2-mark.png" width={32} height={32} alt="4{music}2 brand mark" priority/></span><div className="brand-lockup"><b><em>the</em> decks</b><small>BY <strong>4{'{'}MUSIC{'}'}2</strong></small></div></div><nav className="desktop-status"><span>PRIVATE CLOUD</span><span>{tracks.length} TRACKS</span><span>{user.name}</span></nav><div className="top-actions"><div className="deck-toggle"><button className={deckCount===2?'active':''} onClick={()=>setDeckCount(2)}>2 DECK</button><button className={deckCount===4?'active':''} onClick={()=>setDeckCount(4)}>4 DECK</button></div><button className="icon-button" onClick={()=>authClient.signOut().then(()=>location.href='/sign-in')} aria-label="Sign out"><LogOut/></button></div></header><nav className="mobile-nav"><button className={mobilePanel==='decks'?'active':''} onClick={()=>setMobilePanel('decks')}><Disc3/>Decks</button><button className={mobilePanel==='library'?'active':''} onClick={()=>setMobilePanel('library')}><Library/>Library</button><button className={mobilePanel==='ai'?'active':''} onClick={()=>setMobilePanel('ai')}><Bot/>AI</button></nav><section className={`performance-grid mode-${deckCount} ${mobilePanel!=='decks'?'mobile-hidden':''}`}><div className="mobile-deck-tabs">{Array.from({length:deckCount},(_,i)=><button key={i} className={mobileDeck===i?'active':''} onClick={()=>setMobileDeck(i)}>DECK {String.fromCharCode(65+i)}</button>)}</div><div className="decks-grid">{decks.slice(0,deckCount).map((d,i)=><div className={mobileDeck===i?'mobile-active':''} key={i}><DeckView index={i} deck={d} audio={el=>{audioRefs.current[i]=el}} onDeck={n=>update(i,n)} onLoad={t=>load(i,t)} tracks={tracks}/></div>)}</div><section className="panel mixer-panel"><header><span>MIXER</span><SlidersHorizontal/></header>{decks.slice(0,deckCount).map((d,i)=><label key={i}>DECK {String.fromCharCode(65+i)}<input type="range" min="0" max="1" step="0.01" value={d.volume} onChange={e=>update(i,{...d,volume:Number(e.target.value)})}/></label>)}<label>CROSSFADER<input type="range" min="0" max="100" value={crossfade} onChange={e=>setCrossfade(Number(e.target.value))}/></label></section></section><section className="workspace-lower"><section className={`library-panel ${mobilePanel!=='library'?'mobile-hidden':''}`}><header className="library-header"><div><p className="eyebrow"><Library/>PRIVATE LIBRARY</p><h2>Your tracks</h2></div><label className="search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search your library"/></label><input ref={fileRef} hidden type="file" accept="audio/*" onChange={e=>{const f=e.target.files?.[0];if(f)upload(f);e.target.value=''}}/><button className="primary-action" disabled={uploading} onClick={()=>fileRef.current?.click()}>{uploading?<LoaderCircle className="spin"/>:<CloudUpload/>}{uploading?'Uploading':'Upload audio'}</button></header>{isLoading?<div className="empty-state"><LoaderCircle className="spin"/><b>Loading your private library</b></div>:error?<div className="empty-state"><b>Library unavailable</b><p>{error.message}</p></div>:filtered.length===0?<div className="empty-state"><Disc3/><b>{tracks.length?'No matching tracks':'Your library is empty'}</b><p>{tracks.length?'Try another search.':'Upload audio you own or are authorized to use.'}</p><button className="primary-action" onClick={()=>fileRef.current?.click()}><Plus/>Add your first track</button></div>:<div className="real-track-list">{filtered.map((t,i)=><article key={t.id}><span>{String(i+1).padStart(2,'0')}</span><div><b>{t.title}</b><small>{t.artist} · {time(t.duration||0)}</small></div><span>{t.bpm?.toFixed(1)||'—'} BPM</span><span>{t.musicalKey||'—'}</span><div className="load-actions">{Array.from({length:deckCount},(_,d)=><button key={d} onClick={()=>{load(d,t);setMobileDeck(d);setMobilePanel('decks')}} aria-label={`Load ${t.title} to Deck ${String.fromCharCode(65+d)}`}>{String.fromCharCode(65+d)}</button>)}<button className="delete" onClick={()=>remove(t.id)} aria-label={`Delete ${t.title}`}><Trash2/></button></div></article>)}</div>}</section><aside className={`ai-panel ${mobilePanel!=='ai'?'mobile-hidden':''}`}><header><span><Bot/></span><div><p>GEMINI PERFORMANCE ASSISTANT</p><b>Grounded in your library</b></div></header><div className="assistant-response"><Sparkles/><p>{assistant}</p></div><form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);ask(String(f.get('prompt')));e.currentTarget.reset()}}><textarea name="prompt" placeholder="What should I play next?" required/><button className="primary-action" disabled={asking}>{asking?<LoaderCircle className="spin"/>:<Bot/>}Ask Gemini</button></form><small>Recommendations use only your private library metadata. Audio is not sent to Gemini.</small></aside></section><footer><span>WEB AUDIO <b>READY</b></span><span>STORAGE <b>PRIVATE</b></span><span className="ml-auto">SIGNED IN <b>{user.email}</b></span></footer></main>}
